import { TagCloud } from "react-tagcloud";

export default function WordCloudOutput(props) {
  const { output } = props;
  const defaultRenderer = (tag, size, color) => {
    return (
      <span>
      </span>
    );
  };
}
